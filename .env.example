# Required for AI chat. Keep this value server-side and never commit a real key.
OPENAI_API_KEY=

# OpenAI-compatible providers can override the endpoint.
# OpenRouter example: https://openrouter.ai/api/v1
OPENAI_BASE_URL=

OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_OUTPUT_TOKENS=900
