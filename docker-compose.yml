version: "3.8"
services:
  opspilot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=\
      - OPENAI_MODEL=gpt-4o
    restart: always